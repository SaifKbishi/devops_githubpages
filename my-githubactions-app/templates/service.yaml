apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-githubactions-app.fullname" . }}
  labels:
    {{- include "my-githubactions-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-githubactions-app.selectorLabels" . | nindent 4 }}
